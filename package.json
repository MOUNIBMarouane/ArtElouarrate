{
  "name": "elouarate-art",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node server-clean.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
